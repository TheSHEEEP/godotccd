#ifndef __CCD_CONFIG_H__
#define __CCD_CONFIG_H__

// This file exists only to "pretend" that libccd was built normally (we actually just integrate its source files)
// It only defines our float precision (single, as that is what Godot uses)
#define CCD_SINGLE

#endif /* __CCD_CONFIG_H__ */

